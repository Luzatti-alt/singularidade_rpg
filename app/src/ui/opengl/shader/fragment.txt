#version 330 core

in vec3 fragmentColor;
in vec2 fragment_tex_coord;
out vec4 finalColor;

uniform sampler2D my_texture; //uniform tipo var global
void main(){
    finalColor = vec4(fragmentColor,1.0) 
               * texture(my_texture, fragment_tex_coord);
}